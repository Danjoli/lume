<x-admin.guest-layout title="Recuperar senha"><div class="rounded-xl bg-white p-8"><h1 class="text-2xl font-bold">Esqueci minha senha</h1><p class="mt-2 text-sm text-slate-600">Informe seu e-mail administrativo.</p>@if(session('status'))<div class="mt-4 rounded-lg bg-green-50 p-3 text-sm text-green-700">{{ session('status') }}</div>@endif<form method="POST" action="{{ route('admin.password.email') }}" class="mt-6 space-y-4">@csrf<input class="w-full rounded-lg border p-3" name="email" type="email" value="{{ old('email') }}" placeholder="E-mail" required autofocus>@error('email')<p class="text-xs text-red-600">{{ $message }}</p>@enderror<button class="w-full rounded-lg bg-slate-900 px-4 py-3 font-semibold text-white">Enviar link</button></form><a class="mt-5 block text-center text-sm" href="{{ route('admin.login') }}">Voltar ao login</a></div></x-admin.guest-layout>
