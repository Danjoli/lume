<x-admin.guest-layout title="Redefinir senha"><div class="rounded-xl bg-white p-8"><h1 class="text-2xl font-bold">Criar nova senha</h1><form method="POST" action="{{ route('admin.password.update') }}" class="mt-6 space-y-4">@csrf<input type="hidden" name="token" value="{{ $token }}"><input class="w-full rounded-lg border p-3" name="email" type="email" value="{{ old('email', $email) }}" required><input class="w-full rounded-lg border p-3" name="password" type="password" placeholder="Nova senha" required><input class="w-full rounded-lg border p-3" name="password_confirmation" type="password" placeholder="Confirmar senha" required>@foreach($errors->all() as $error)<p class="text-xs text-red-600">{{ $error }}</p>@endforeach<p class="text-xs text-slate-500">Mínimo de 12 caracteres, com maiúscula, minúscula, número e símbolo.</p><button class="w-full rounded-lg bg-slate-900 px-4 py-3 font-semibold text-white">Redefinir senha</button></form></div></x-admin.guest-layout>
